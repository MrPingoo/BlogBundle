{% extends '@JlayBlogBundle/Resources/views/backend.html.twig' %}

{% block body -%}

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Liste des snippets
            </h1>
            <ol class="breadcrumb">
                <li>
                    <i class="fa fa-dashboard"></i>  <a href="{{ path('jlay_blog_backend_index') }}">Dashboard</a>
                </li>
                <li class="active">
                    <i class="fa fa-folder"></i> Liste des snippets
                </li>
                <li>
                    <a href="{{ path('snippet_new') }}" class="float-left"><i class="fa fa-plus"></i> Créer un nouveau snippet</a>
                </li>
            </ol>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">

                {% for entity in entities %}
                    <div class="post-preview">
                        <a href="{{ path('snippet_show', { 'id': entity.id }) }}">
                            <h2 class="post-title">
                                {{ entity.title }}
                            </h2>
                        </a>
                        <p class="post-meta"><a href="{{ path('snippet_edit', { 'id': entity.id }) }}" ><i class="fa fa-edit"></i> Modifier</a></p>
                    </div>
                    <hr>
                {% endfor %}

                {#
                <!-- Pager -->
                <ul class="pager">
                    <li class="next">
                        <a href="{{ path('snippet') }}" >Anciens articles →</a>
                    </li>
                </ul>
                #}
            </div>
        </div>
    </div>

{% endblock %}
